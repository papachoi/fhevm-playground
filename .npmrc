# safer, reproducible installs
save-exact=true
engine-strict=true

# npm audit on by default (you can still run with --omit=dev when needed)
audit=true
fund=false

# default to ignoring lifecycle scripts locally; override in CI per-step if needed
ignore-scripts=true
